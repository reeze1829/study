/*
 * 주제: 4-1. 함수의 정의와 호출 (function-basic)
 * 날짜: 2026-03-06
 * 내용: 
 * [함수 설계의 핵심 질문]
 * 1. 매개변수(Parameter) = "재료": 소괄호 () 안에 뭐가 써져 있다면 "재료를 준비해라"라는 뜻!
 * 2. 반환형(Return Type) = "결과물": void면 "빈손으로 돌아옴", int/float면 "해당 타입의 물건을 들고 돌아옴"!
 *
 * 실습: 4가지 함수 유형(재료와 결과물의 유무) 분석 및 호출 메커니즘 이해
 */

#include <stdio.h>
                                                                                          // 계산기 스타일
int add(int a, int b) {                       // 입력 O, 출력 O : 계산기 스타일            // 재료가 필요한가? YES (int a, int b) -> 더할 숫자 두 개가 있어야 함
    return a + b;                             // 결과를 main으로 던져줌 (int형 반환)       // 결과물이 필요한가? YES (return) -> 더한 결과값을 나에게 돌려줘야 함
}
                                                                                          // 단순 호출 스타일
void hello() {                                // 입력 X, 출력 X : 단순 명령 수행 스타일    // 재료가 필요한가? NO ( ) -> 그냥 정해진 문구만 출력하면 됨
    printf("시스템: 함수 호출 성공!\n");      // void는 돌려줄 게 없다는 뜻!               // 결과물이 필요한가? NO (void) -> 출력만 하고 보고할 내용(값)은 없음
}
                                                                                               // 보고서 출력 스타일
void check_score(int score) {                 // 입력 O, 출력 X : 전달받은 값으로 출력만 수행   // 재료가 필요한가? YES (int score) -> 검사할 점수를 알려줘야 함
    if (score >= 80) printf("결과: 합격입니다.\n");                                            // 결과물이 필요한가? NO (void) -> 합격/불합격 출력만 하고 끝냄
    else printf("결과: 불합격입니다.\n");     // 안에서 일만 처리하고 끝냄 
}
                                                                                               // 정보 요청 스타일
int get_lucky_number() {            // 입력 X, 출력 O : 정해진 값을 가져오는 스타일             // 재료가 필요한가? NO ( ) -> 재료 없이 그냥 달라고만 하면 됨
    return 7;                       // 호출만 하면 무조건 7을 뱉어냄                            // 결과물이 필요한가? YES (return) -> 약속된 행운의 숫자를 돌려줘야 함
}

int main() {
    printf("함수 기초 종합 실습 \n\n");

    hello();                        // 단순 호출: 잠시 hello 함수로 갔다 돌아옴
  
    int sum = add(10, 20);          // 10, 20을 전달하고 결과(30)를 sum에 저장
    printf("더하기 결과: %d\n", sum);

    check_score(85);                // 85라는 재료만 던져주고 결과는 거기서 바로 확인

    printf("오늘의 행운의 숫자: %d\n", get_lucky_number());    // 입력은 필요 없지만 뱉어주는 값(7)이 있으니 바로 출력에 사용 가능
    printf("3. 5 + 7 = %d (직접 호출)\n", add(5, 7));         // 함수 안에 함수: add(5, 7)이 먼저 실행되어 12가 되고, 그게 printf에 전달됨

    return 0;
}
